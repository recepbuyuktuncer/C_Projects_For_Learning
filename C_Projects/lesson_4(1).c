//Calculating the area and perimeter of a square

#include<stdio.h>
#include<stdlib.h>

int main (){

	int side_length, area_of_square, perimeter_of_square;

	printf("Side Length: ");
	scanf("%d",&side_length);

	printf("Area of Square: %d\nPerimeter of Square: %d", side_length*side_length, side_length*4);


}
